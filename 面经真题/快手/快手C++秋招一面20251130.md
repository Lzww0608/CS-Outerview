# 快手C++秋招一面20251130

1. 虚函数实现原理
2. 虚表是一个类有一个还是一个对象有一个？
3. 查询虚表的时间复杂度是多少？
4. `std::move()` 原理，涉及移动吗？
5. 假设有一个1KB的大对象，move能节省拷贝吗？
6. 智能指针原理
7. new和malloc有什么区别？
8. 用new生成的对象，可以用free释放吗？那如果是基础类型呢？
9. 用new创建数组时，释放的时候需要写出元素个数吗？
10. `std::map` 和 B+ Tree 有什么区别？
11. 红黑树和B+ Tree在性能、内存空间占用上的对比
12. 为什么数据库选择B+ Tree而不是红黑树？
13. 在STL里，内存池是怎么实现的，有怎样的结构？
14. 执行 `vector<int> v(4, 100)` 会发生什么，在栈上还是堆上分配？
15. 那如果是 `new vector<int>(4,100)` 呢？
16. 如何拿到类中私有成员变量的值？
17. 有一个二维数组里面都有值，想要给每个数都加100，行遍历和列遍历有什么区别？
18. 在浏览器中访问一个HTTP服务器，这里面会经过哪些协议？
19. 为什么不直接用TCP协议，还需要用HTTP协议？
20. 算法：`1,2,3,4,...,n` 构造二叉树
21. 算法：合并两个有序数组a和b，两个数组可能是升序或降序，合并后的结果放在a中，合并后的顺序按照a的顺序来
