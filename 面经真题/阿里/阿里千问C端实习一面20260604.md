1. 先做一下自我介绍。
2. 向量检索引擎解决什么场景下的问题？
3. 为什么要实现 WAL / KV 内存索引 / HNSW / SIMD 这些功能？
4. 崩溃恢复是否任何状态下都能恢复？能否恢复到崩溃前状态？
5. `fsync` 的影响了解过吗？
6. `fsync` 和 `fflush` 有什么区别？
7. 为什么要调用 `fsync`？
8. 写文件用什么函数？打开文件用什么函数？
9. CRC32 校验的是什么数据？
10. 为什么选择 CRC32？
11. CRC32 碰撞情况怎么样？实际测试见过碰撞吗？
12. 存储吞吐量和性能怎么样？
13. SIMD 的 3～4 倍提升具体是相比什么提升？
14. SIMD 具体怎么写？是手写汇编还是其他方式？
15. SIMD 处理数据时怎么处理？
16. 除了 SIMD 还有什么优化？
17. 64 字节对齐为什么能提升性能？不对齐会怎样？
18. 怎么让内存 64 字节对齐？
19. 优化是否已经到极致？还有必要继续优化吗？
20. 和 FAISS / Milvus 差距多少？测过吗？
21. 项目里挑战最大的是哪一块？
22. 怎么知道 cache 是否失效？
23. 怎么验证崩溃恢复正确性？
24. 标题里写 AI Agent Memory，和 Agent 有什么关系？
25. 了解 AI Agent 吗？
26. Agent 怎么知道有哪些工具可以用？怎么告诉大模型？
27. 大模型多轮对话记忆怎么做？
28. 检索是关键字检索还是语义检索？为什么向量化后能检索？
29. 工具调用时模型是在等工具返回吗？没返回会一直等吗？
30. 工具结果怎么丢给模型？第二次请求有什么不同？
31. 连续多工具调用导致 token 越来越多怎么办？
32. 模型层或服务层有什么优化？KV Cache 怎么做？
33. 常用 STL 有哪些？
34. `vector` 插入元素的方法有哪些？
35. `push_back` 和 `emplace_back` 有什么区别？参数类型一样吗？
36. `emplace_back` 如何在 `vector` 内部原地构造？
37. `reserve` 和 `resize` 有什么区别？
38. 空 `vector` 执行 `resize(1000)` 会构造 1000 次吗？怎么避免开销？
39. 有哪些智能指针？哪些可以复制构造？
40. `shared_ptr` 拷贝构造做了什么？
41. `weak_ptr` 怎么知道 `shared_ptr` 是否还活着？
42. 引用计数在 `shared_ptr` 对象里面吗？
43. strong count 归零后控制块是否立即 delete？
44. 基类指针如何调用到子类虚函数？
45. 虚函数表里有多个函数，怎么知道调用哪个？
46. 基类和子类声明顺序不一样怎么办？
47. 多继承下两个基类都有虚函数怎么办？
48. 用过哪些 AI Coding 工具？
49. 是否完整用 AI 做项目？能否完全信任 AI？
50. AI Coding 任务：设计并实现后端配置热更新模块，支持不重启服务动态加载本地配置文件 JSON / YAML / INI，并触发业务回调。
51. 如何保证 AI 写的代码可用？