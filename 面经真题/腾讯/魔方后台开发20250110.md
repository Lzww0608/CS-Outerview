# 魔方后台开发20250110

1. 一面（电话面）
2. 上来二十分钟的项目介绍、收获。
3. #define与const的区别
4. `#define`预处理，文本替换，无类型安全检查，全局替换
5. 编译器处理，常量，运行时分配内存，不可改变
6. class与struct的区别，没答到关键点（默认权限）
7. 默认权限
8. RAII
9. 虚函数与默认函数， POD
10. class怎么防止继承，只讲了私有构造，忘了final关键字
11. 继承`nocopy`类（私有构造）
12. final
13. 根据最大元素对齐
14. `memmove`处理重叠的情况，设置临时缓冲区，双向复制
15. STL库介绍一下，为什么要设计迭代器
16. 安全检查
17. map的实现原理，红黑树高度差最大是多少
18. 红黑树
19. 两倍（当前节点为黑）
20. 运行期多态（虚函数表）与编译器多态（模板）
21. 非对称加密算法流程
22. 常用签名算法
23. `RSA`：大数因子分解
24. `DSA`：离散对数问题
25. `ECDSA`：椭圆曲线离散对数问题
26. 磁盘使用情况df、fdisk
27. 端口使用情况netstate
28. netstat -ltnp
29. ss -ltn
30. lsof -i
31. CPU信息
32. top` `htop
33. 命令别名alias
34. TCP与UDP的区别
35. 粘包与拆包，标识头和缓冲处理
36. 消息长度前缀
37. 固定消息格式
38. 固定前后缀 （XML）
39. 应用层协议， 比如TCP不解决粘包问题
40. 二面（视频面）
41. 很实在，直接开始手撕代码：将零元素移动到数组尾部，保持其他元素的相对顺序
42. LeetCode移动零
43. 如果用STL库，应该用哪个函数，std::remove
44. sizeof(vector)多大，sizeof(vector)又是多大？
45. unordered_map的底层实现原理
46. 哈希
47. 三面（总监面）
48. 步，不在视野内的玩家不进行同步
49. 尬聊
50. 尬聊，
51. 同步范围管理
52. 数据压缩
53. 最后还是问问C++相关的，map的实现原理
54. 使用虚函数返回一个ID来实现，判断成功则使用std::static_cast转换
55. 3月底就接到了腾讯的offer call
