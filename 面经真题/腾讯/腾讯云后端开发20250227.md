# 腾讯云后端开发20250227

1. 25一面
2. TCP的RST有什么用，在哪里用到了
3. 拒绝连接请求、拒绝无效报文段
4. 中止现有连接
5. 处理半打开连接
6. 端口扫描
7. 防火墙
8. 知道ICMP吗
9. 差错报告：数据包在传输过程中遇到错误，路由器或者目标主机返回ICMP差错报文给源主机
10. shell解析
11. 输入输出重定向
12. 退出状态码 `$?`获取
13. fork具体过程(不会，说只知道写时复制)
14. 复制父进程的PCB，
15. `exec()`调用
16. fork()返回不同的值，父进程返回子进程的ID，子进程返回0（区分不同进程）
17. Java的volatile关键字
18. MySQL：
19. Redis：
20. Kafk
21. MTU是什么
22. MySQL里覆盖索引是什么
23. 手撕：leetcode 34. 在排序数组中查找元素的第一个和最后一个位置
24. 26二面
25. TIME_WAIT有什么用
26. 被动关闭端发送为发送的消息
27. 接收未收到的消息
28. 又问了RST
29. traceroute用过没（没有）
30. 追踪路由路径
31. epoll和select有什么区别
32. epoll红黑树记录事件，事件驱动
33. 什么时候用水平触发 什么时候用边缘触发（不知道）
34. 一个TCP句柄什么情况下会有可读事件（不知道）
35. 监听socket：连接到达
36. 接收到新数据
37. 连接关闭
38. TCP的connect发送了什么报文
39. 可以，绑定地址和端口，立即返回。
40. fork都会返回吗（不知道）
41. 成功的话 父进程返回子进程的ID，子进程返回0
42. 失败的话 父进程返回-1，并设置errno
43. zookeeper了解吗（不了解）
44. 一致性哈希算法和普通哈希算法有什么区别
45. C++的vector的size()和capacity()的区别
46. 如何改变size的大小
47. 如何改变capacity的大小
48. 单例模式怎么实现
49. 怎么实现一个线程一个单例
50. 又问了MySQL的覆盖索引
51. 为什么主键要用自增id
52. 唯一性，简单
53. http请求由哪几部分构成
54. 常见的请求头有什么（一开始把请求头当成请求方法了，后来面试官指正）
55. Host: 指定服务器的主机名和端口号（如果不是默认端口）。
56. If-Modified-Since: 客户端缓存相关， 如果在此时间后资源有更新，则服务器返回更新内容，否则返回304 Not Modified
57. If-None-Match: 客户端缓存相关, 如果资源的ETag和这个值不匹配，则返回更新内容，否则返回 304 Not Modified
58. GET和POST的区别
59. Linux用过什么命令
