1. UUID相比于雪花算法生成的ID，劣势是什么？
2. 在Python中我想实现某个接口的SSE流式返回，代码如何实现举个例子？
3. 介绍一下TCP/IP的五层架构。
4. 想抓HTTP/HTTPS的包，需要怎么做？
5. 说一下进程和线程的区别。
6. 说一下栈和队列的区别。
7. 假如有10个线程，数组长度也为10，不同的线程i访问数组i的位置，这种情况下需要加锁吗？
8. 那么线程i写入对应数组i的值，但它要读不同地方的值，这种情况需要加锁吗？
9. Redis这边用过哪些常见的数据结构？
10. MySQL有个SQL执行比较慢，你怎么去分析它？
11. 简单介绍一下Dockerfile怎么写？
12. Prometheus、Grafana简单介绍一下？
13. 介绍一下你日常git管理的方法？


算法题：
1. 为下面的场景设计数据库表：对于一个书籍管理系统，需要存储书和作者的对应关系，一本书可能有多个作者，一个作者可能也有多个不同的名字（例如“鲁迅”和“周树人”）。
2. 路经总和。（LeetCode 113）