# 腾讯WXG一面20250906

1. 自我介绍
2. 实习转正情况
3. Raft开始
4. 介绍raft
5. 何时发起leader选举
6. 如何避免选举失败（超时时长随机化）
7. raft集群有没有不可用的时间，比如日志提交时？无，高可用，日志写入期间向leader读老日志
8. 为什么要大于N/2的节点确认后才提交日志，小于N/2行吗？不向从节点确认行吗？
9. raft如何保障leader的日志是最新的raft这块结束，猫猫个人觉得答得还行，没有被某个问题卡住
10. 网络开始
11. 读过nginx源码没咋可能读过（
12. TCP长连接和短连接，什么时候用
13. 低频次、无状态、非交互式的场景用短连接，其它的一般长连接
14. 交互频率低、周期性长
15. 并发量巨大，但是单次请求快
16. 场景：
17. 嵌入式 或 IoT设备
18. DNS查询
19. OS开始
20. C++和Go的区别，从服务端编程的角度讲讲
21. C++和Go的协程实现对比
22. 协程什么时机切换
23. 隐式的协作式切换（最常见）
24. 显式的协作式切换
25. 开发者可以手动建议调度器进行切换。
26. 抢占式调度（Go的“杀手锏”）
27. 为了防止某个CPU密集型的Goroutine（例如一个没有任何阻塞操作的超长循环）饿死其他Goroutine，Go实现了抢占机制。
28. 协程比线程的优势
29. 协程切换，需要保存哪些上下文？
30. 无栈协程实现时会将**挂起点 + 局部变量**放到堆上的**协程帧**中
31. 寄存器中保存了哪些信息？
32. 上下文保存在哪里
33. 函数调用时如何切换上下文的
34. 函数调用信息保存在栈帧中，通过栈帧指针实现函数跳转
35. 具体一点，说说函数调用的过程，哪些信息保存在哪里，哪些是调用者保存的哪些是被调用者保存的
36. 虚拟内存
37. TLB为什么快
38. TLB可以缓存，页表查找是需要多级映射
39. mmap分配的内存在哪
40. 栈和堆中间的内存映射区
41. 内存布局，内核态和用户态在哪
42. 为什么进程切换比线程慢
43. OS到此结束
