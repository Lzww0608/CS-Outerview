1.项目拷打（感觉面试官并不懂这个项目，问的很奇怪）
2.sizeof()的作用，sizeof一个数组和一个指针的区别，sizeof是什么时候计算出来的
3.类的大小和类中所有元素的大小之和相等吗，内存对齐是什么，为什么要内存对齐
4.虚函数原理，如何通过虚函数表指针和虚函数表找到目标函数地址，虚函数表指针存在哪儿
5.父类指针指向子类对象后，子类对象是怎么样调用虚函数的。我又重述了一遍虚函数表和虚函数表指针的过程。然后反复问我子类对象是怎么访问到虚函数表指针的？？？（不懂这个问题在问什么）
6.vector的扩容机制，数据是怎么复制的
7.如果vector最终有L个元素，按两倍扩容机制算，那么整个过程中一共扩容了多少次，复制了多少个元素，写出计算公式
8.unordered map和map的区别与使用场景
9.unordered map的原理、时间复杂度
10.针对unordered map的链表（桶）深入拷打：如果一共有L个元素，N个桶，那复杂度是多少？平均复杂度是L/N吗？这里不太会，说了句应该也会扩容来保持O1，然后顺着我继续问怎么实现扩容，扩容倍数是多少合适，我说将每个链表头结点移到新的内存地址中，也扩容两倍。继续问第一个头结点移动后仍然在第一个吗，如果不是的话该怎么设计呢？（这里已经有点问懵了没想到追着这个不放）就回答要取决于具体的哈希函数的设置，可能仍然在第一个（感觉这个回答也不算错吧，但面试官又问我了一遍这个问题，给我整不会了。）最后问如果key是个非常长的字符串那么时间复杂度会受到影响吗，如果有影响你会怎么缓解这个问题（彻底答不上来了，查了一下是要考我分布式计算？）
11.问map的时间复杂度
12.由于红黑树是树结构的，那么迭代器如何顺序遍历map呢。答中序遍历，然后问中序遍历的话下一个元素是当前节点的父节点、左节点还是右节点，并手撕中序遍历
13.互斥锁和自旋锁的区别，使用场景
14.获取不到互斥锁而睡眠的线程存放在哪里，线程睡眠后醒来的机制（没看过底层源码不知道）