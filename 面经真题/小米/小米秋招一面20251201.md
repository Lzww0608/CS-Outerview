# 小米秋招一面20251201

1. 介绍一下实习。
2. 说一下项目。
3. 说一下实习内容。
4. 场景题：做 P2P 时需要在 UDP 上传输数据，偶尔发生丢包，如何设计一个 SDK 来保证可靠传输？
5. 场景题：在解决传输问题后，借鉴 TLS/SSL 握手机制，如何设计一套端对端的密钥方案？
6. 手撕题：假设有一个高并发键值存储系统，底层使用非易失性内存，如何将随机插入转化为批量、顺序写入，同时保证 `insert` 在并发环境下线程安全且不使用全局锁，`flush` 能高效地一次性顺序刷写？
