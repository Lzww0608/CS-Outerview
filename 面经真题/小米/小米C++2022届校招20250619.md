# 小米C++2022届校招20250619

1. 虚拟内存相关详细讲一下。
2. 讲讲左值和右值。
3. 什么时候使用右值？
4. 什么是完美转发？
5. 假如 `a` 是 `T` 的左值引用，`T` 是 `int&&` 类型，那么 `a` 实际上是什么？
6. 讲一下智能指针。
7. `shared_ptr` 和 `unique_ptr` 的区别以及性能对比是什么？
8. `weak_ptr` 的作用是什么？
9. `shared_ptr` 是线程安全的吗？
10. lambda 表达式有哪些捕获类型？
11. 讲讲多态及实现机制。
12. 什么是虚基类？
13. 多继承的时候，虚函数表指针怎么存？
14. `using std::cin` 和 `using namespace std` 后使用 `cin` 有什么区别？
15. 什么是元编程？
16. 详细介绍一下 MSRA 实习项目。
17. 交易预测的输入和输出需要存储，这个空间消耗大概有多大？
18. 一个有环链表里，两个速度不一样的指针移动，起始位置也不一定一样，它们一定相遇吗？
19. 找出数据中最小的 `k` 个数。
20. 自我介绍。
21. 对自动驾驶系统涉及的研发方向中，你对哪个感兴趣？
22. 发现性能瓶颈时使用过什么方法？
23. 如何发现死锁？
24. 在开发时制定什么样的规则可以避免死锁？
25. 如何调试内存泄漏？
26. 如何调试 core dump？
27. 介绍一下虚拟内存。
28. 每个进程的虚拟内存有多大？
29. 如果物理内存大于 4G，可以不使用虚拟内存吗？
30. 线程切换要进入内核态吗？
31. 一个很大的二维数组存在内存中，按行读取快还是按列读取快？
32. `map` 和 `unordered_map` 有什么区别？
33. `unordered_map` 使用什么方法解决 hash 冲突？
34. 实现 LRU，要求自己写双向链表。
