限流有哪些常见算法？
固定窗口，滑动窗口，漏桶算法，令牌桶算法的优缺点是什么？
你最熟悉的语言是哪一个？
Go 语言中 defer 是什么时候执行的？多个 defer 的执行顺序是怎样的？defer 能不能修改函数返回值？
如果有上万字符串拼接，能否直接用 “+”？为什么？
大量字符串拼接应该用什么方式？为什么？
如果要存储一批商品并判断是否存在，应该用什么数据结构？
map 的 key 和 value 应该如何设计？
空结构体 struct{} 还有什么用途？
Go 的 map 能否并发读写？
如果需要并发读写 map，应该怎么做？
sync.Map 和普通 map 有什么区别？
goroutine 和线程有什么区别？
goroutine 是如何被调度执行的？
解释一下 GMP 模型中 G、M、P 分别是什么？
goroutine 创建之后会被放到哪里？
P 的数量是如何确定的？
每个 P 有几个队列？
如果本地队列满了会怎么办？
如果一个 P 的队列空了会怎么办？
什么是 work stealing？
Go 的垃圾回收机制是什么？
写屏障是为了解决什么问题？
MySQL 中事务的特性是什么？
如果一个事务中第二条 SQL 执行失败，会发生什么？最终数据是多少？
Redis 中事务的执行机制是什么？
Redis 事务失败会回滚吗？
Redis 事务执行过程中是否会被其他命令插入？
MySQL 有哪些索引类型？
什么是聚簇索引？
什么是非聚簇索引？
聚簇索引和非聚簇索引的区别是什么？
MySQL 索引底层是什么数据结构？
B+ 树的特点是什么？
B+ 树为什么只在叶子节点存数据？
B+ 树相比 B 树有什么优势？
为什么数据库使用 B+ 树而不是 B 树？
范围查询（如 between）在 B+ 树中是如何执行的？
如果用普通二叉搜索树，范围查询怎么做？
二叉树的遍历方式有哪些？
前序、中序、后序遍历分别是什么？
中序遍历的结果有什么特点？
二叉搜索树查找的时间复杂度是多少？
二叉树最坏情况下会发生什么？
为什么二叉搜索树会退化？
如何避免二叉树退化？
红黑树和 AVL 树的时间复杂度是多少？
有序数组查找的时间复杂度是多少？
有序数组插入的时间复杂度是多少？
在什么场景下选择数组，什么场景下选择树？
有没有一种结构既支持快速插入又支持范围查询？