# 微软C++面经20250716

1. 介绍一下项目。
2. `new` 与 `malloc` 的区别是什么？为什么 `new` 会调用构造函数？
3. 虚函数与纯虚函数的区别是什么？
4. 有什么防止内存泄漏的办法？
5. 有没有办法修改 `const` 值？
6. C++ 的内存布局是什么？
7. 给定一个二叉搜索树和一个 `val`，找出与 `val` 最接近的节点。
8. 给定一个递增序列，左边数字集合通过 `|` 折叠形成右边序列，给定右边序列，如何返回左边集合？
9. 介绍一下项目。
10. `static` 的作用和用法是什么？
11. 多态的应用场景有哪些？如何实现多态？
12. 你知道哪些 C++ 新特性？
13. 智能指针的原理是什么？
14. 给定一个 `vector<int>` 和一个值，如何判断数组中是否存在 3 个数之和等于给定值？
15. 给定 `s1`、`s2` 两个字符串，如何在 `s1` 中找到一个包含 `s2` 的连续子串，且子串中字符顺序不必与 `s2` 相同？
16. 介绍一下项目。
17. 输入一个 `n*m` 的 0/1 矩阵，如何返回包含所有 1 的最小子矩阵？
18. C++ 内存布局是什么？
19. `sizeof` 与 `strlen` 的区别是什么？
20. 用 C 语言实现 `strcpy`。
21. 给定一个由电话号码英文单词形式组成的字符串，如 `onethreefive`，如何将其转换成电话号码数字形式？
