# 微软苏州C++20250715

1. 介绍一下你做的东西或项目。
2. 求 1 到 1000000 的质数和。
3. 实现一个 stack，要求 `O(1)` 返回最小值。
4. 实现一个 queue，要求平均 `O(1)` 返回最小值。
5. 用两个 queue 实现一个 stack。
6. 求一个字符串的最长回文子串。
7. 如何判断一个字典中是否存在某个单词的变异体？
8. 给定一个二叉树结点，如何找到它的中序遍历下的下一个结点？（有指向父结点的指针）
9. 一次可以取 1~3 个数，对方也一次取 1~3 个数，且对方采用最优策略，问自己能取到的最大值是多少？
10. 你最大的项目收获是什么？
11. 你是什么专业？
12. 给定一个 `m*n` 的矩阵，如何求从左上角到右下角的最小值？
13. 股票买卖问题中，如果只允许买卖一次，如何求最大收益？
