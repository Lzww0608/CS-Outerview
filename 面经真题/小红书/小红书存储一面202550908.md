# 小红书存储一面202550908

1. 介绍一下 `pwrite` 的整个流程。
2. 顺序读场景下 `direct I/O` 和 `buffered I/O` 的性能对比是什么？
3. `page cache` 的 prefetch 机制了解吗？
4. `page cache` 的大小是由谁决定的？
5. 数据拷贝过程从磁盘到 CPU 的 L1 Cache 是怎样的？
6. 用过异步 I/O 吗？
7. 同步 I/O 的情况下 CPU 的使用情况是怎样的？可以介绍一下 DMA 吗？
8. `io_uring` 相比 AIO 性能好在哪里？主要是哪些方面减少了开销？
9. `io_uring` 的 polling 模式和 SPDK 怎么比较？
10. 说一下项目中的性能测试部分，SSD 中的 cache 会对性能产生影响吗，这块是怎么处理的？
11. 介绍一下 B+Tree 和 LSM-Tree。
12. B+Tree 并发控制中的 `latch crabbing` 是什么？
13. B+Tree 和 LSM-Tree 各自的读写放大情况是什么？
14. LSM-Tree 的 `delete` 操作中，墓碑什么时候能被删除？
15. 大量 `delete` 对性能的影响是什么？有哪些解决方案？
16. 说一下 LSM-Tree 的 KV 分离。
17. KV 分离的负面影响有哪些？有什么解决方案？
18. KV 分离导致 Scan 性能下降的问题，目前学术界有什么解决方案？
19. RocksDB 的 `WriteBatch` 如何保证原子性？项目中又是如何保证原子性的？
20. RocksDB 的一致性读是什么？`memtable` 中如何保证一致性读？
21. Raft 大论文中的 PreVote 是什么？
22. Raft 需要持久化的信息有哪些？`voteFor` 丢失会怎么样？
23. 什么是 Joint Consensus？
24. `std::move` 是什么？
25. `shared_ptr` 的线程安全是怎么实现的？
26. 内存序有哪些？对于 `release-acquire`，`happens-before` 是如何建立的？
27. 二分搜索数值的范围。
