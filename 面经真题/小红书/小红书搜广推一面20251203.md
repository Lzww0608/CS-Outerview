# 小红书搜广推一面20251203

1. 介绍一下项目。
2. 介绍一下实习。
3. 你是怎么做调试的？直接上手 GDB 吗？
4. 仅仅针对内存层面，一种代码有好几种写法，功能差不多，但性能可能差异很大，需要注意哪些问题？
5. 你了解过哪些锁或同步机制？这些锁的应用场景分别是什么？
6. 有一段 LIT 字符比如 `LLITL`，只能在相邻不同字符插入 `L`、`I`、`T` 且不能超过 `2n` 次，给出每一步插入位置，使得最后字符串的每个字符个数一致。
