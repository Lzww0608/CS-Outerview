# 小红书一面挂经20250708

1. `vector::push_back` 的时间复杂度是什么？
2. 什么是半连接队列？半连接队列满了怎么办？
3. Linux 中如何实现一个能精确到微秒的定时器？
4. LT 和 ET 的区别是什么？如何用 LT 处理可写事件？
5. 给你一个二叉树，如何保存到磁盘，方便下次读取？
6. 写一个 Web 服务，如何减少中间层读写消耗的内存资源？
7. 使用 `stdlib.h` 中的 `rand()` 函数，估算 `π` 的值。
8. `1` 到 `n` 按照字典序排序，不能暴力。
9. 两个线程交替打印。
