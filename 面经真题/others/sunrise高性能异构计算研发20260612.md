- 手撕：合并有序数组
- 介绍一下向量数据库 / 向量检索引擎的整体设计。
- KV 存储是怎么实现的？
- KV 写入、读取、删除、崩溃恢复的大致流程是什么？
- WAL 一直追加，文件会不会越来越大？当前有没有整理或回收机制？
- 如果后续做 WAL compaction，内存索引里的 offset 失效怎么办？
- compaction 过程中读写请求怎么办？服务是否需要停下来？
- 你了解过其他类似的单机存储引擎吗？比如 Bitcask、LevelDB、RocksDB？
- RPC 项目可以介绍一下吗？具体做了哪些模块和工作？
- Reactor 模型是什么？Main-Sub Reactor 是什么样的模型？
- Reactor 模型在什么场景下会有瓶颈？
- 如果请求很小、业务逻辑很轻，但 I/O 和拆包很多，这种场景要怎么优化？
- 如果 I/O 线程只负责读，不负责解析，Length-Prefix 半包怎么办？什么时候才能分发？
- 如果新增解码线程组，为什么不直接在解码线程里完成轻量业务，还要再分发给 worker？
- 线程之间传递消息或者同步，一般用什么方法？
- 你项目里跨线程投递任务是怎么做的？
- 跨线程唤醒 EventLoop 可以用什么机制？
- 手撕代码：合并两个有序整数数组，返回一个有序数组。
- 讲一下合并有序数组的思路。
- 这段程序的时间复杂度和空间复杂度是多少？
- 代码里的 `vector::push_back` 做了什么？
- `vector` 扩容时会发生什么？
- `vector` 扩容申请内存时，操作系统层面会做什么？
- 平时写程序出问题了，一般怎么调试？
- 你对 AI 推理方面有什么了解？你刚开始提到 vLLM，是有了解吗？
- vLLM 是在 RAG 项目里用到的吗？
- 你的向量检索引擎在 RAG 过程中只是做检索吗？有没有做模型推理？
- 你对 AI 推理和 CUDA 有什么了解？
- 你现在实习时间是只能暑假，还是开学后也可以继续？
- 除了曦望，你还在推进哪些公司或岗位？