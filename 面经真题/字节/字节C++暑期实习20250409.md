# 字节C++暑期实习20250409

1. 找数组中的前 k 大个数，有哪些方法？分析时间复杂度和空间复杂度，并实现其中一种
2. 海量 IP 中，给定一个 IP，输出它出现的次数，分析时间复杂度和空间复杂度
3. `select`、`poll`、`epoll` 的区别，什么时候用 `select` 和 `poll`
4. `sizeof`
5. C++ 内存模型，常量存储区、静态变量和全局变量
6. 虚函数的实现
7. UDP 适用于什么场景？
8. TCP 和 UDP 可以绑定到同一个端口吗？如何实现？
9. HTTP 协议里 `Connection` 选项的作用
10. 如何优化一条 SQL 查询语句？
11. MySQL 有哪些索引？
12. 联合索引和最左匹配原理
13. Redis 有哪些淘汰策略？
14. 概率题：一对夫妻一直生到女孩为止，最后男孩女孩的比例是多少？男孩个数的期望是多少？
15. 概率题：如何使用 `rand7` 实现 `rand5`？如何使用 `rand5` 实现 `rand7`？
16. `socket` 编程的流程，`accept` 调用和三次握手的关系，`accept` 什么时候会阻塞，`accept` 队列由谁维护？
17. 一个服务如何提高并发？
18. 场景题：一千台服务器两两连接，服务器间延迟不断变化，如何确定任意两台服务器之间的最短路径？
