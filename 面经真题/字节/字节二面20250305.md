# 字节二面20250305

1. 如果通过 SDK 的方式暴露给开发人员使用，但代码发生变动后别人都需要更新包，有什么方法能让其他人无感知升级？
2. 调用配置中心时，客户端是怎么感知到服务端配置发生变化的？
3. 如果让你自己实现，该怎么做？用哪种进程间通信方式？
4. 这里是要用一个线程或进程去死循环查询这个队列吗？
5. 如果是线程的话，是怎么获取到那个地址的数据的？
6. 有没有接触过底层进程间通信的代码开发？
7. 匿名管道和文件管道的区别
8. `fork` 的时候是否会创建 fd？
9. 配置中心服务端是怎么主动推送消息的？
10. 推送消息用事件驱动怎么做？
11. `epoll` 原理
12. 如果 Redis 是多主节点并分布在不同机房，同步规则是什么？
13. 写入的时候是只写最近机房的集群吗？其他机房集群有写入功能吗，还是只是读取？
14. 两段实习中有没有遇到 SQL 查询优化的问题？
15. 为什么会引起索引失效？最左前缀法则是什么？
16. 从 B+ 树的角度讲一下，如果插入节点时达到了页上限，树结构怎么调整？
17. 算法：删除无效括号
