# 字节一面8.16

1. `static` 关键字，在 `.h` 文件里加 `static` 和不加 `static` 的区别
2. `extern` 关键字
3. 虚函数原理
4. 有和没有虚函数的类大小怎么计算？一个对象只能有一个虚表指针吗？
5. 没有定义拷贝构造函数时编译器一定会生成吗？什么情况下不会生成？
6. 介绍智能指针
7. 项目难点：介绍 gRPC 原理
8. 算法题：实现 `unique_ptr`
