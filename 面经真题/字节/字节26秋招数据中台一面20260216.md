# 字节26秋招数据中台一面20260216

1. 不是计算机专业的，自己学过什么？
2. 操作系统怎么读取磁盘文件？
3. 知道文件地址后，怎么知道文件在哪里？
4. 指针偏移量存在哪里？
5. 什么是非阻塞 I/O？
6. 有哪几种非阻塞 I/O？
7. `epoll` 怎么和非阻塞 I/O 配合？为什么阻塞 I/O 不行？
8. `epoll` 和 `select` 的区别、用途区别？
9. `select` 用在什么场景？
10. `select` 有什么弊端？
11. `epoll` 用在什么场景？
12. `epoll` 有什么弊端？
13. `epoll` 的水平触发和边缘触发有什么区别？
14. 边缘触发有什么弊端？适合什么场景？水平触发呢？
15. 怎么解决边缘触发可能读丢数据的情况？
16. `socket` 怎么保证一次性读取完数据？
17. 手撕：单词接龙
