# 字节一二三面20250415

1. `vector` 的扩容怎么实现的？
2. 迭代器删除所用的函数
3. `vector` 用迭代器删除后迭代器会不会失效？
4. 什么情况适合用 `vector`，什么情况适合用 `list`？
5. C++ 迭代器都有哪些？
6. 讲下 `const`
7. `const int *p` 和 `int *const p` 的区别
8. 不同位置的 `static` 变量的区别
9. `static` 一般什么时候用？
10. `static` 在编译器层面的实现
11. `shared_ptr`
12. 如果要自己设计一个 `shared_ptr`，要包含哪些方法？
13. `new` 和 `malloc` 的区别
14. `malloc` 底层实现
15. C++ 程序的内存分布
16. 介绍 C++ 多态
17. 虚函数的底层实现
18. 构造函数可不可以是虚函数？
19. 为什么构造函数不能是虚函数？
20. 析构函数可不可以是虚函数？
21. 虚函数表在什么时候初始化？
22. TCP 报文的首部有哪些字段？
23. HTTPS 的加密
24. 怎么保持登录状态？
25. `session` 的底层实现
26. 虚拟内存、共享内存、驻留内存
27. 静态链接和动态链接
28. 使索引生效，SQL 查询该语句怎么写？
29. 用过什么中间件？
30. 堆排时间复杂度，稳不稳定？
31. 快排稳不稳定？
32. 概率题：和面试官抛硬币，一人一次，面试官先抛，先抛到正面朝上的胜，问面试官获胜的概率
33. 有一个随机数生成器，生成 0 的概率是 `p`，生成 1 的概率是 `1-p`，要求用这个生成器实现等概率生成 0 和 1
34. 写代码：给一串数字串，将数字串转化为 IP 地址，给出所有可能结果
35. 写代码：二叉搜索树权值第 k 小
36. 写代码：给一段未缩进的 XML 代码，输出缩进后的结果
37. 物理内存和虚拟内存区别
38. 物理地址和虚拟地址区别
39. 为什么要用虚拟地址？
40. 如果只有 2GB 内存，有一个 4GB 大小的程序，怎么运行？
41. 如果不使用虚拟内存，能不能运行成功？为什么能运行成功，怎么做到？
42. 介绍一下协程
43. 僵尸进程是什么？
44. 文件描述符是什么？
45. 有一个等概率生成 `0-4` 的随机数生成器，用它实现等概率生成 `0-6`
46. 代码题：给一个字符串，求最多包含 `k` 种字符的最长子串长度
47. 代码题：给一棵二叉树和一个点集合，求集合内所有点的 LCA
