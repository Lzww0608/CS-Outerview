# 字节C++后端提前批20250516

1. 介绍一下 WebServer 项目
2. `const int* a`、`int* const a`、`int const* a` 的区别
3. 智能指针介绍一下，`auto_ptr` 现在还在用吗？
4. 讲一下 TCP，三次握手，能不能两次？
5. TCP 粘包拆包，怎么解决？
6. 介绍 `socket` 和 `epoll`、I/O 模型
7. `epoll` 的优点，与 `select` 和 `poll` 的区别
8. 什么场景下用 `select`、`poll`、`epoll`
9. `epoll` 怎么解决 I/O 效率问题？内核和用户空间之间怎么传递消息？
10. 死锁产生的条件
11. 算法：最长上升子序列，要求 `O(nlogn)`
12. 算法：右边第一个更大的数，要求 `O(n)`
13. C++ 跟 C 的区别？
14. 智能指针有几种？
15. `auto_ptr` 被摒弃的原因？
16. 其他三种智能指针是什么？
17. C++ 的设计模式有哪些？
18. 介绍一下单例模式
19. 用到的工具类有哪些？STL
20. STL 中 `sort` 的内部实现
21. 有哪些数据结构，能说多少说多少
22. Hash 冲突怎么解决？
23. Hash 函数有了解吗？
24. 操作系统里的进程和线程的区别？
25. 线程安全了解吗？
26. 死锁的几个条件？
27. OSI 到 TCP/IP，为什么从 OSI 转向 TCP/IP？
28. 应用层协议有哪些？
29. HTTPS 了解吗？怎么建立连接？
30. 算法：Top K
31. 算法：归并排序，归并用的什么思想？
32. `socket` 服务端从建立连接到结束用到了哪些系统调用？
33. `epoll` 是什么模式，为什么要 I/O 多路复用？
34. `epoll` 原理
35. LT 和 ET 模式的区别
36. 哪些开源软件用到了 I/O 多路复用？
37. 系统调用讲一下，具体细节
38. 内核态和用户态的区别
39. 哪些方式会从用户态进入到内核态？
40. 写一个 MySQL 语句，取第 K 大的行
41. HTTPS 的原理、握手过程、如何加密、CA、对称加密、非对称加密
42. 算法：根据二叉树中序和后序构建前序
43. 算法：木棍问题
