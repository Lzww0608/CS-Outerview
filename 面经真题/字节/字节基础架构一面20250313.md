# 字节基础架构一面20250313

1. 使用过哪些 C++11 以上新特性？
2. `shared_ptr` 的底层实现
3. `shared_ptr` 解决了普通指针的什么问题？
4. 是否能设计一个 `shared_ptr`？里面有哪些成员变量？
5. `weak_ptr` 是否使用过？具体 API 怎么调用？能否设计一个 `weak_ptr`？
6. `unique_ptr` 的使用场景
7. 进程间通信有哪些方式？
8. 消息队列怎么实现？
9. 本地套接字的 API 调用过程
10. 线程同步方式
11. 互斥锁底层实现
12. 线程阻塞和休眠怎么实现？
13. 设计题：主线程创建一个资源，在子线程消费完之前 `wait` 住
