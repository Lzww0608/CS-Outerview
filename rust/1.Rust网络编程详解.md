# Rust 网络编程详解

## 一、Rust 网络编程概述

### 1.1 Rust 网络编程特点

```text
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  Rust 网络编程核心优势：                                    │
│                                                             │
│  1. 内存安全：无 GC，编译期检查内存                         │
│  2. 零成本抽象：高性能，无运行时开销                        │
│  3. 并发安全：所有权系统防止数据竞争                        │
│  4. 异步支持：async/await 原生支持                          │
│  5. 丰富的生态： Tokio、 mio、 tonic 等                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、TCP/UDP 编程

### 2.1 标准库 TCP

```rust
use std::net::{TcpListener, TcpStream};
use std::io::{Read, Write};

// TCP 服务器
fn main() -> std::io::Result<()> {
    let listener = TcpListener::bind("127.0.0.1:8080")?;
    
    println!("Server listening on :8080");
    
    for stream in listener.incoming() {
        match stream {
            Ok(stream) => {
                // 为每个连接创建新线程
                std::thread::spawn(|| {
                    handle_connection(stream);
                });
            }
            Err(e) => eprintln!("Connection failed: {}", e),
        }
    }
    Ok(())
}

fn handle_connection(mut stream: TcpStream) {
    let mut buffer = [0; 1024];
    
    // 读取客户端数据
    match stream.read(&mut buffer) {
        Ok(n) => {
            println!("Received: {}", String::from_utf8_lossy(&buffer[..n]));
            
            // 发送响应
            let response = b"HTTP/1.1 200 OK\r\n\r\nHello!";
            stream.write_all(response).unwrap();
        }
        Err(e) => eprintln!("Read error: {}", e),
    }
}
```

### 2.2 TCP 客户端

```rust
use std::net::TcpStream;
use std::io::{Read, Write};

fn main() -> std::io::Result<()> {
    // 连接服务器
    let mut stream = TcpStream::connect("127.0.0.1:8080")?;
    
    // 发送数据
    stream.write_all(b"Hello from client!")?;
    
    // 读取响应
    let mut buffer = [0; 1024];
    let n = stream.read(&mut buffer)?;
    println!("Server said: {}", String::from_utf8_lossy(&buffer[..n]));
    
    Ok(())
}
```

### 2.3 UDP 编程

```rust
use std::net::{UdpSocket};

fn main() -> std::io::Result<()> {
    // 创建 UDP socket
    let socket = UdpSocket::bind("127.0.0.1:8080")?;
    
    println!("UDP Server listening on :8080");
    
    // 接收数据
    let mut buffer = [0; 1024];
    let (amt, src) = socket.recv_from(&mut buffer)?;
    
    println!("Received {} bytes from {}", amt, src);
    println!("Data: {}", String::from_utf8_lossy(&buffer[..amt]));
    
    // 发送响应
    socket.send_to(b"ACK", src)?;
    
    Ok(())
}
```

---

## 三、异步网络编程

### 3.1 Tokio 异步运行时

```toml
# Cargo.toml
[dependencies]
tokio = { version = "1", features = ["full"] }
```

```rust
use tokio::net::{TcpListener, TcpStream};
use tokio::io::{AsyncReadExt, AsyncWriteExt};

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    // 异步 TCP 服务器
    let listener = TcpListener::bind("127.0.0.1:8080").await?;
    
    println!("Async TCP server listening");
    
    loop {
        let (mut socket, addr) = listener.accept().await?;
        println!("New connection from: {}", addr);
        
        // 处理连接
        tokio::spawn(async move {
            let mut buf = [0; 1024];
            
            loop {
                let n = match socket.read(&mut buf).await {
                    Ok(n) if n == 0 => return,
                    Ok(n) => n,
                    Err(e) => {
                        eprintln!("Error: {}", e);
                        return;
                    }
                };
                
                if socket.write_all(&buf[..n]).await.is_err() {
                    return;
                }
            }
        });
    }
}
```

### 3.2 tokio-stream 流处理

```rust
use tokio::net::{TcpListener, TcpStream};
use tokio::io::{AsyncReadExt, AsyncWriteExt};
use tokio_stream::wrappers::TcpListenerStream;
use tokio_stream::StreamExt;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let listener = TcpListener::bind("127.0.0.1:8080").await?;
    let mut incoming = TcpListenerStream::new(listener);
    
    while let Some(stream) = incoming.next().await {
        match stream {
            Ok(stream) => {
                tokio::spawn(handle_connection(stream));
            }
            Err(e) => eprintln!("Error: {}", e),
        }
    }
    
    Ok(())
}
```

---

## 四、常用网络库

### 4.1 mio（底层 I/O）

```toml
[dependencies]
mio = "0.8"
```

```rust
use mio::{Events, Interest, Poll, Token};
use mio::net::{TcpListener, TcpStream};
use std::time::Duration;

const SERVER: Token = Token(0);
const CLIENT: Token = Token(1);

fn main() -> std::io::Result<()> {
    let mut poll = Poll::new()?;
    let mut events = Events::with_capacity(128);
    
    // 创建监听 socket
    let mut server = TcpListener::bind("127.0.0.1:8080".parse().unwrap())?;
    poll.registry().register(&mut server, SERVER, Interest::READABLE)?;
    
    println!("Server registered with mio");
    
    loop {
        // 等待事件
        poll.poll(&mut events, Some(Duration::from_secs(1)))?;
        
        for event in &events {
            match event.token() {
                SERVER => {
                    // 有新连接
                    let (connection, address) = server.accept()?;
                    println!("New connection: {}", address);
                    
                    let mut stream = connection;
                    poll.registry().register(
                        &mut stream, 
                        CLIENT, 
                        Interest::READABLE | Interest::WRITABLE
                    )?;
                }
                _ => println!("Unknown token"),
            }
        }
    }
}
```

### 4.2 tonic（gRPC）

```toml
[dependencies]
tonic = "0.8"
prost = "0.11"
tokio = { version = "1", features = ["full"] }
```

```rust
// proto/helloworld.proto
/*
syntax = "proto3";
package helloworld;

service Greeter {
    rpc SayHello (HelloRequest) returns (HelloReply);
}

message HelloRequest {
    string name = 1;
}

message HelloReply {
    string message = 1;
}
*/

use tonic::{transport::Server, Request, Response, Status};

pub mod hello {
    tonic::include_proto!("helloworld");
}

use hello::{greeter_server::{Greeter, GreeterServer}, HelloReply, HelloRequest};

#[derive(Default)]
struct MyGreeter;

#[tonic::async_trait]
impl Greeter for MyGreeter {
    async fn say_hello(
        &self,
        request: Request<HelloRequest>,
    ) -> Result<Response<HelloReply>, Status> {
        let name = request.into_inner().name;
        let reply = HelloReply {
            message: format!("Hello {}!", name),
        };
        Ok(Response::new(reply))
    }
}

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let addr = "[::1]:50051".parse().unwrap();
    let greeter = MyGreeter::default();
    
    Server::builder()
        .add_service(GreeterServer::new(greeter))
        .serve(addr)
        .await?;
    
    Ok(())
}
```

### 4.3 actix-web（HTTP 框架）

```toml
[dependencies]
actix-web = "4"
actix-rt = "2"
```

```rust
use actix_web::{web, App, HttpServer, HttpResponse};

async fn index() -> HttpResponse {
    HttpResponse::Ok().body("Hello World!")
}

async fn greet(web::Path(name): web::Path<String>) -> HttpResponse {
    HttpResponse::Ok().body(format!("Hello {}!", name))
}

#[actix_web::main]
async fn main() -> std::io::Result<()> {
    HttpServer::new(|| {
        App::new()
            .route("/", web::get().to(index))
            .route("/{name}", web::get().to(greet))
    })
    .bind("127.0.0.1:8080")?
    .run()
    .await
}
```

### 4.4 reqwest（HTTP 客户端）

```rust
use reqwest;
use tokio;

#[tokio::main]
async fn main() -> Result<(), reqwest::Error> {
    let client = reqwest::Client::new();
    
    // GET 请求
    let response = client.get("https://api.github.com/users")
        .header("User-Agent", "rust-client")
        .send()
        .await?;
    
    println!("Status: {}", response.status());
    println!("Body: {}", response.text().await?);
    
    // POST 请求
    let response = client.post("https://httpbin.org/post")
        .json(&serde_json::json!({
            "name": "rust",
            "features": ["fast", "safe"]
        }))
        .send()
        .await?;
    
    println!("POST Response: {}", response.status());
    
    Ok(())
}
```

---

## 五、WebSocket 编程

### 5.1 tokio-tungstenite

```toml
[dependencies]
tokio = { version = "1", features = ["full"] }
tokio-tungstenite = "0.18"
futures-util = "0.3"
```

```rust
use futures_util::{SinkExt, StreamExt};
use tokio::net::{TcpListener, TcpStream};
use tokio_tungstenite::{accept_async, tungstenite::Message};

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let addr = "127.0.0.1:8080";
    let listener = TcpListener::bind(addr).await?;
    
    println!("WebSocket server listening on {}", addr);
    
    while let Ok((stream, addr)) = listener.accept().await {
        println!("New WebSocket connection: {}", addr);
        tokio::spawn(handle_connection(stream));
    }
    
    Ok(())
}

async fn handle_connection(stream: TcpStream) {
    let ws_stream = accept_async(stream).await.expect("Error during handshake");
    
    let (mut write, mut read) = ws_stream.split();
    
    while let Some(msg) = read.next().await {
        match msg {
            Ok(Message::Text(text)) => {
                println!("Received: {}", text);
                // 回显消息
                if write.send(Message::Text(text)).await.is_err() {
                    break;
                }
            }
            Ok(Message::Close(_)) => break,
            Err(e) => {
                eprintln!("Error: {}", e);
                break;
            }
            _ => {}
        }
    }
}
```

---

## 六、常用库总结

| 库名 | 用途 | 特点 |
|------|------|------|
| std::net | 标准库 TCP/UDP | 简单，阻塞模型 |
| mio | 跨平台 I/O | 事件驱动，Tokio 基础 |
| tokio | 异步运行时 | 完整异步生态 |
| actix-web | HTTP 框架 | 高性能 |
| axum | HTTP 框架 | 轻量，现代化 |
| tonic | gRPC | Protobuf 支持 |
| reqwest | HTTP 客户端 | 异步 HTTP |
| tokio-tungstenite | WebSocket | 异步 WebSocket |
| rustls | TLS/SSL | 安全连接 |
| lapin | AMQP/RabbitMQ | 消息队列 |

---

## 七、面试常见问题

### Q1: Rust 网络编程和 Go 相比有什么优势？

Rust 优势：1）内存安全无 GC，延迟更稳定；2）所有权系统防止数据竞争；3）零成本抽象，性能更好。Go 优势：1）语法简单，并发模型容易；2）部署简单。

### Q2: Tokio 的工作原理？

Tokio 是事件驱动的异步运行时，使用 mio 监听 I/O 事件，工作线程池处理任务。async/await 将协程编译成状态机，任务在 I/O ready 时恢复执行。

### Q3: 如何选择同步还是异步？

需要高并发、低延迟选异步（Tokio）；简单脚本、CPU 密集选同步（std::net）。注意不要混用阻塞调用和非阻塞运行时。

---

## 八、总结

1. **标准库**：std::net 提供基础 TCP/UDP
2. **异步生态**：Tokio 是主流异步运行时
3. **HTTP 框架**：actix-web、axum
4. **gRPC**：tonic
5. **WebSocket**：tokio-tungstenite
